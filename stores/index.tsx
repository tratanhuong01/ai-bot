// import { create } from "zustand";

// type AppState = {
//   updateData: (key: keyof Omit<AppState, "updateData">, value: any) => void; // Hàm update
// };

// const useStore = create<AppState>((set) => ({
//   categories: [],
//   sliders: [],
//   menuActive: false,
//   updateData: (key: keyof Omit<AppState, "updateData">, value: any) =>
//     set((state) => ({ ...state, [key]: value })),
// }));

export default () => {};
